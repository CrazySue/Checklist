// Checklist v0.9.9 最终修复（原地更新，版本保持 0.9.9）
const fs = require('fs');
const path = require('path');
const FILE = path.join(__dirname, '..', 'Checklist-v0.9.9.html');

let html = fs.readFileSync(FILE, 'utf8');
let fail = 0;
function apply(oldStr, newStr, expect) {
  const c = html.split(oldStr).length - 1;
  if (c !== expect) { console.error('COUNT MISMATCH (expect ' + expect + ', got ' + c + '): ' + JSON.stringify(oldStr.slice(0, 90))); fail++; return; }
  html = html.split(oldStr).join(newStr);
}

// ================= #1 安卓导出：权限申请 + IS_PENDING 标准流程 + openFileDescriptor 双保险 =================
apply(
"function handleExport(){\n  const editing = Store.state._editing;\n  const cl = Store.state.checklists.find(c=>c.id===editing);\n  if(!cl){ showToast(t('export_fail')); return; }\n  const filename = (cl.name || 'checklist').replace(/[\\\\/:*?\"<>|]/g,'_') + '.checklist';\n  const json = buildExportPayload(cl);\n  // HBuilderX 5+ App（APK）\n  if(window.plus && plus.os && plus.os.name==='Android' && plus.android){\n    // 1) java.io.FileWriter 直写公共 Download 目录（路径字符串直传，桥接兼容性最好）\n    if(exportViaFileWriter(filename, json)) return;\n    // 2) MediaStore 直写（Android 10+ 免权限）\n    if(exportViaPlusAndroid(filename, json)) return;\n    // 3) 系统\"另存为\"对话框（SAF，默认下载目录，权限由系统保证）\n    exportViaSAF(filename, json);\n    return;\n  }\n  if(window.plus && plus.io){\n    // 其他 plus 环境：plus.io 链（_downloads → PUBLIC_DOWNLOADS → 私有文档目录）\n    if(exportViaPlusIO(filename, json)) return;\n  }\n  downloadViaAnchor(json, filename);\n}",
"function handleExport(){\n  const editing = Store.state._editing;\n  const cl = Store.state.checklists.find(c=>c.id===editing);\n  if(!cl){ showToast(t('export_fail')); return; }\n  const filename = (cl.name || 'checklist').replace(/[\\\\/:*?\"<>|]/g,'_') + '.checklist';\n  const json = buildExportPayload(cl);\n  // HBuilderX 5+ App（APK）\n  if(window.plus && plus.os && plus.os.name==='Android' && plus.android){\n    exportViaAndroid(filename, json);\n    return;\n  }\n  if(window.plus && plus.io){\n    // 其他 plus 环境：plus.io 链（_downloads → PUBLIC_DOWNLOADS → 私有文档目录）\n    if(exportViaPlusIO(filename, json)) return;\n  }\n  downloadViaAnchor(json, filename);\n}\n/* Android 导出主流程：先申请存储权限（Android 6-10 设备），再依次尝试 FileWriter / MediaStore / SAF */\nfunction exportViaAndroid(filename, json){\n  const run = ()=>{\n    if(exportViaFileWriter(filename, json)) return;\n    if(exportViaPlusAndroid(filename, json)) return;\n    exportViaSAF(filename, json);\n  };\n  try{\n    plus.android.requestPermissions(['android.permission.WRITE_EXTERNAL_STORAGE'], function(){ run(); }, function(){ run(); });\n  }catch(e){\n    run();\n  }\n}",
1);
apply(
"function exportViaPlusAndroid(filename, json){\n  if(!window.plus || !plus.android || !plus.os || plus.os.name!=='Android') return false;\n  try{\n    const main = plus.android.runtimeMainActivity();\n    const Downloads = plus.android.importClass('android.provider.MediaStore$Downloads');\n    const ContentValues = plus.android.importClass('android.content.ContentValues');\n    const Environment = plus.android.importClass('android.os.Environment');\n    const values = new ContentValues();\n    values.put(Downloads.DISPLAY_NAME, filename);\n    values.put(Downloads.MIME_TYPE, 'application/octet-stream');\n    values.put(Downloads.RELATIVE_PATH, Environment.DIRECTORY_DOWNLOADS);\n    const resolver = main.getContentResolver();\n    const uri = resolver.insert(Downloads.EXTERNAL_CONTENT_URI, values);\n    if(!uri) return false;\n    const os = resolver.openOutputStream(uri);\n    if(!os) return false;\n    const PrintWriter = plus.android.importClass('java.io.PrintWriter');\n    const pw = new PrintWriter(os);\n    pw.print(json);\n    pw.close();\n    showToast(t('export_success'));\n    return true;\n  }catch(e){\n    return false;\n  }\n}",
"function exportViaPlusAndroid(filename, json){\n  if(!window.plus || !plus.android || !plus.os || plus.os.name!=='Android') return false;\n  let uri = null;\n  try{\n    const main = plus.android.runtimeMainActivity();\n    const Downloads = plus.android.importClass('android.provider.MediaStore$Downloads');\n    const ContentValues = plus.android.importClass('android.content.ContentValues');\n    const Environment = plus.android.importClass('android.os.Environment');\n    const resolver = main.getContentResolver();\n    const values = new ContentValues();\n    values.put(Downloads.DISPLAY_NAME, filename);\n    values.put(Downloads.MIME_TYPE, 'application/octet-stream');\n    values.put(Downloads.RELATIVE_PATH, Environment.DIRECTORY_DOWNLOADS);\n    // Android 10+ 标准流程：先标记 pending，写入完成后再取消标记\n    values.put(Downloads.IS_PENDING, 1);\n    uri = resolver.insert(Downloads.EXTERNAL_CONTENT_URI, values);\n    if(!uri) return false;\n    let ok = false;\n    // 方式一：openOutputStream\n    const os = resolver.openOutputStream(uri);\n    if(os){\n      const PrintWriter = plus.android.importClass('java.io.PrintWriter');\n      const pw = new PrintWriter(os);\n      pw.print(json);\n      pw.close();\n      ok = true;\n    }else{\n      // 方式二：openFileDescriptor + FileOutputStream + OutputStreamWriter（部分设备 openOutputStream 返回 null）\n      const pfd = resolver.openFileDescriptor(uri, 'w');\n      if(!pfd) return false;\n      const FileOutputStream = plus.android.importClass('java.io.FileOutputStream');\n      const OutputStreamWriter = plus.android.importClass('java.io.OutputStreamWriter');\n      const fos = new FileOutputStream(pfd.getFileDescriptor());\n      const osw = new OutputStreamWriter(fos, 'UTF-8');\n      osw.write(json);\n      osw.close();\n      pfd.close();\n      ok = true;\n    }\n    // 写入完成，取消 pending 标记\n    const done = new ContentValues();\n    done.put(Downloads.IS_PENDING, 0);\n    resolver.update(uri, done, null, null);\n    if(ok){ showToast(t('export_success')); return true; }\n    return false;\n  }catch(e){\n    // 失败时清理残留条目\n    if(uri){\n      try{ const main = plus.android.runtimeMainActivity(); main.getContentResolver().delete(uri, null, null); }catch(e2){}\n    }\n    return false;\n  }\n}",
1);
apply(
"        const uri = data.getData();\n        if(!uri){ showToast(t('export_fail')); return; }\n        const os = main.getContentResolver().openOutputStream(uri);\n        if(!os){ showToast(t('export_fail')); return; }\n        const PrintWriter = plus.android.importClass('java.io.PrintWriter');\n        const pw = new PrintWriter(os);\n        pw.print(json);\n        pw.close();\n        showToast(t('export_success'));",
"        const uri = data.getData();\n        if(!uri){ showToast(t('export_fail')); return; }\n        const resolver = main.getContentResolver();\n        let ok = false;\n        // 方式一：openOutputStream\n        const os = resolver.openOutputStream(uri);\n        if(os){\n          const PrintWriter = plus.android.importClass('java.io.PrintWriter');\n          const pw = new PrintWriter(os);\n          pw.print(json);\n          pw.close();\n          ok = true;\n        }else{\n          // 方式二：openFileDescriptor + FileOutputStream + OutputStreamWriter\n          const pfd = resolver.openFileDescriptor(uri, 'w');\n          if(pfd){\n            const FileOutputStream = plus.android.importClass('java.io.FileOutputStream');\n            const OutputStreamWriter = plus.android.importClass('java.io.OutputStreamWriter');\n            const fos = new FileOutputStream(pfd.getFileDescriptor());\n            const osw = new OutputStreamWriter(fos, 'UTF-8');\n            osw.write(json);\n            osw.close();\n            pfd.close();\n            ok = true;\n          }\n        }\n        if(ok){ showToast(t('export_success')); }else{ showToast(t('export_fail')); }",
1);

// ================= #2 主题切换：所有元素颜色统一平滑过渡 =================
apply(
"*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent;}",
"*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent;}\n/* 主题切换：所有元素颜色统一平滑过渡（元素自身声明 transition 的规则优先） */\n*{transition-property:background-color,color,border-color,background;transition-duration:.25s;}",
1);

// ================= #4 顶栏标题：整个标题（名字+「检查单」）一起上下翻动，应用于所有页面 =================
apply(
"let slideTitleNext = false; // 检查单间切换时标题做位移动画\nlet slideTitleDir = 'right'; // 目标检查单方向（right=右侧/下一个，left=左侧/上一个）",
"let titleFlipDir = 'fade'; // 顶栏标题翻动方向（up=页面从左向右翻入，down=从右向左翻入，fade=淡入淡出）",
1);
apply(
"  slideTitleNext = (!fromOtherPage && oldId !== clId);\n  slideTitleDir = goRight ? 'right' : 'left';\n  renderTopbar('home');",
"  if(!fromOtherPage && oldId !== clId) titleFlipDir = goRight ? 'up' : 'down';\n  renderTopbar('home');",
1);
apply(
"      setTopbarTitle(cl.name, needSpace ? ' ' + suffixText : suffixText, slideTitleNext ? ('slide-'+slideTitleDir) : 'fade');\n      slideTitleNext = false;",
"      setTopbarTitle(cl.name, needSpace ? ' ' + suffixText : suffixText, (titleFlipDir==='up'||titleFlipDir==='down') ? 'flip-'+titleFlipDir : 'fade');\n      titleFlipDir = 'fade';",
1);
apply(
"    setTopbarTitle(editing? t('edit_checklist') : t('new_checklist'), '');",
"    setTopbarTitle(editing? t('edit_checklist') : t('new_checklist'), '', (titleFlipDir==='up'||titleFlipDir==='down') ? 'flip-'+titleFlipDir : 'fade');\n    titleFlipDir = 'fade';",
1);
apply(
"    setTopbarTitle(t('settings'), '');",
"    setTopbarTitle(t('settings'), '', (titleFlipDir==='up'||titleFlipDir==='down') ? 'flip-'+titleFlipDir : 'fade');\n    titleFlipDir = 'fade';",
1);
apply(
"function setTopbarTitle(text, suffix, mode){\n  const wrap = $('#topbarTitle');\n  const tEl = $('#topbarTitleText');\n  const sEl = $('#topbarTitleSuffix');\n  if(mode!=='slide-right' && mode!=='slide-left' && tEl.textContent===text && sEl.textContent===suffix) return;\n  if((mode==='slide-right'||mode==='slide-left') && sEl.textContent){\n    // 检查单间切换：名字（XX）上下翻动（目标在右侧→向上翻，在左侧→向下翻），\n    // 旧名翻出后立即翻入新名（中间无空白）；翻到约 75% 时后缀（「检查单」）\n    // 按 MD3 曲线非线性移动到新位置；名字完全相同时也播放翻动动画\n    const dirUp = mode==='slide-right';\n    const oldLeft = sEl.getBoundingClientRect().left;\n    // 测量后缀新位置（临时换名测量，同步恢复，不产生视觉闪动）\n    const oldText = tEl.textContent;\n    tEl.textContent = text;\n    const newLeft = sEl.getBoundingClientRect().left;\n    tEl.textContent = oldText;\n    const delta = newLeft - oldLeft;\n    const hasMove = Math.abs(delta) > 0.5;\n    // 1. 旧名翻出（120ms）\n    tEl.style.display = 'inline-block';\n    tEl.style.transition = 'transform 120ms var(--md-easing-emphasized-accelerate), opacity 120ms var(--md-easing-standard-accelerate)';\n    tEl.style.transform = 'translateY('+(dirUp?-1:1)*100+'%)';\n    tEl.style.opacity = '0';\n    setTimeout(()=>{\n      // 2. 同一帧换名 + 后缀位移补偿（视觉连续），随后新名立即翻入（无空白）\n      tEl.style.transition = 'none';\n      tEl.style.transform = 'translateY('+(dirUp?1:-1)*100+'%)';\n      tEl.style.opacity = '0';\n      tEl.textContent = text;\n      if(hasMove){\n        sEl.style.transition = 'none';\n        sEl.style.transform = 'translateX('+(-delta)+'px)';\n      }\n      void tEl.offsetWidth;\n      tEl.style.transition = 'transform 200ms var(--md-easing-emphasized-decelerate), opacity 180ms var(--md-easing-emphasized-decelerate)';\n      tEl.style.transform = 'translateY(0)';\n      tEl.style.opacity = '1';\n      setTimeout(()=>{\n        tEl.style.transition=''; tEl.style.transform=''; tEl.style.opacity='';\n        tEl.style.display='';\n      }, 220);\n    }, 120);\n    // 3. 翻动到约 75%（240ms）处后缀开始非线性移动到新位置\n    setTimeout(()=>{\n      if(hasMove){\n        sEl.style.transition = 'none';\n        sEl.style.transform = 'translateX('+(-delta)+'px)';\n        void sEl.offsetWidth;\n        sEl.style.transition = 'transform 320ms var(--md-easing-emphasized)';\n        sEl.style.transform = 'translateX(0)';\n      }\n      setTimeout(()=>{ sEl.style.transition=''; sEl.style.transform=''; }, 340);\n    }, 240);\n    return;\n  }",
"function setTopbarTitle(text, suffix, mode){\n  const wrap = $('#topbarTitle');\n  const tEl = $('#topbarTitleText');\n  const sEl = $('#topbarTitleSuffix');\n  if(mode!=='flip-up' && mode!=='flip-down' && tEl.textContent===text && sEl.textContent===suffix) return;\n  if(mode==='flip-up' || mode==='flip-down'){\n    // 整个标题（名字+「检查单」）一起上下翻动；\n    // 页面从左向右翻入（后退）→ 向下翻；从右向左翻入（前进）→ 向上翻；同名也播放\n    const dirUp = mode==='flip-up';\n    wrap.style.transition = 'transform 140ms var(--md-easing-emphasized-accelerate), opacity 140ms var(--md-easing-standard-accelerate)';\n    wrap.style.transform = 'translateY('+(dirUp?-1:1)*100+'%)';\n    wrap.style.opacity = '0';\n    setTimeout(()=>{\n      tEl.textContent = text;\n      sEl.textContent = suffix;\n      wrap.style.transition = 'none';\n      wrap.style.transform = 'translateY('+(dirUp?1:-1)*100+'%)';\n      void wrap.offsetWidth;\n      wrap.style.transition = 'transform 240ms var(--md-easing-emphasized-decelerate), opacity 200ms var(--md-easing-emphasized-decelerate)';\n      wrap.style.transform = 'translateY(0)';\n      wrap.style.opacity = '1';\n      setTimeout(()=>{\n        wrap.style.transition=''; wrap.style.transform=''; wrap.style.opacity='';\n      }, 260);\n    }, 140);\n    return;\n  }",
1);
apply(
".topbar{\n  height:56px;display:flex;align-items:center;gap:4px;\n  padding:0 4px 0 16px;\n  background:var(--md-surface);\n  color:var(--md-on-surface);\n  position:relative;z-index:20;\n  transition:background-color .25s ease;\n}",
".topbar{\n  height:56px;display:flex;align-items:center;gap:4px;\n  padding:0 4px 0 16px;\n  background:var(--md-surface);\n  color:var(--md-on-surface);\n  position:relative;z-index:20;overflow:hidden;\n  transition:background-color .25s ease;\n}",
1);
// 表单页内切换（编辑↔新建）：标题向上翻
apply(
"      renderForm();\n      renderTopbar('form');\n      renderBottombar();\n      content.style.transition = 'none';",
"      renderForm();\n      titleFlipDir = 'up';\n      renderTopbar('form');\n      renderBottombar();\n      content.style.transition = 'none';",
1);

// ================= #5 翻页动画：全部改为检查单切换的小幅滑动+淡入淡出 =================
apply(
"function switchPage(target, reverse){\n  const pages = {home:'#pageHome', form:'#pageForm', settings:'#pageSettings'};\n  const curEl = $(pages[currentPage]);\n  const tgtEl = $(pages[target]);\n  if(curEl===tgtEl) return;\n  // 方向规则：新建/设置页从右侧滑入，检查单页从左侧滑入；\n  // reverse（从设置返回）：设置页向右滑出，目标页从左滑入\n  const goRight = reverse ? (target === 'home') : (target !== 'home');\n\n  // 退出页：内联样式滑出（方向可靠，不受类残留/CSS 顺序影响）\n  curEl.classList.remove('page-active','page-enter-right','page-enter-left','page-leave-left','page-leave-right');\n  curEl.classList.add(goRight ? 'page-leave-left' : 'page-leave-right');\n  curEl.style.transition = '';\n  curEl.style.transform = goRight ? 'translateX(-100%)' : 'translateX(100%)';\n  curEl.style.opacity = '0';\n\n  // 进入页：先无过渡定位到起点，强制重排后再滑入\n  tgtEl.classList.remove('page-enter-right','page-enter-left','page-leave-left','page-leave-right','page-active');\n  tgtEl.style.transition = 'none';\n  tgtEl.style.transform = goRight ? 'translateX(100%)' : 'translateX(-100%)';\n  tgtEl.style.opacity = '0';\n  tgtEl.style.pointerEvents = 'none';\n  void tgtEl.offsetWidth; // 强制重排，确保起点生效\n  tgtEl.style.transition = '';\n  tgtEl.style.transform = '';\n  tgtEl.style.opacity = '';\n  tgtEl.style.pointerEvents = '';\n  tgtEl.classList.add('page-active');\n\n  currentPage = target;\n  // 动画结束后只清理退出页的内联样式（leave 类保持，退出页持续隐藏）\n  clearTimeout(switchPage._cleanup);\n  switchPage._cleanup = setTimeout(()=>{\n    curEl.style.transform = ''; curEl.style.opacity = '';\n  }, 450);\n}",
"function switchPage(target, reverse){\n  const pages = {home:'#pageHome', form:'#pageForm', settings:'#pageSettings'};\n  const curEl = $(pages[currentPage]);\n  const tgtEl = $(pages[target]);\n  if(curEl===tgtEl) return;\n  // 方向规则：新建/设置页从右侧滑入，检查单页从左侧滑入；\n  // reverse（从设置返回）：设置页向右滑出，目标页从左滑入\n  const goRight = reverse ? (target === 'home') : (target !== 'home');\n  // 翻页动画与检查单切换一致：小幅滑动 40px + 淡入淡出（旧页滑出→新页滑入）\n  const dir = goRight ? -1 : 1;\n  const enterDir = goRight ? 1 : -1;\n\n  // 1. 退出页滑出（150ms 加速）\n  curEl.classList.remove('page-active','page-enter-right','page-enter-left','page-leave-left','page-leave-right');\n  curEl.classList.add(goRight ? 'page-leave-left' : 'page-leave-right');\n  curEl.style.transition = 'transform 150ms cubic-bezier(.3,0,.8,.15), opacity 120ms cubic-bezier(.3,0,.8,.15)';\n  curEl.style.transform = 'translateX('+dir*40+'px)';\n  curEl.style.opacity = '0';\n\n  // 2. 进入页先无过渡定位到反方向起点，150ms 后滑入（300ms 减速）\n  tgtEl.classList.remove('page-enter-right','page-enter-left','page-leave-left','page-leave-right','page-active');\n  tgtEl.style.transition = 'none';\n  tgtEl.style.transform = 'translateX('+enterDir*40+'px)';\n  tgtEl.style.opacity = '0';\n  tgtEl.style.pointerEvents = 'none';\n  setTimeout(()=>{\n    void tgtEl.offsetWidth;\n    tgtEl.style.transition = 'transform 300ms cubic-bezier(.05,.7,.1,1), opacity 250ms cubic-bezier(.05,.7,.1,1)';\n    tgtEl.style.transform = 'translateX(0)';\n    tgtEl.style.opacity = '1';\n    tgtEl.style.pointerEvents = '';\n    tgtEl.classList.add('page-active');\n    setTimeout(()=>{\n      tgtEl.style.transition = ''; tgtEl.style.transform = ''; tgtEl.style.opacity = '';\n    }, 320);\n  }, 150);\n\n  currentPage = target;\n  // 顶栏标题翻动方向：从左向右翻入（后退）→ 向下翻；从右向左翻入（前进）→ 向上翻\n  titleFlipDir = goRight ? 'up' : 'down';\n  // 动画结束后只清理退出页的内联样式（leave 类保持，退出页持续隐藏）\n  clearTimeout(switchPage._cleanup);\n  switchPage._cleanup = setTimeout(()=>{\n    curEl.style.transform = ''; curEl.style.opacity = '';\n  }, 500);\n}",
1);

console.log('v0.9.9 fixes done, fail =', fail);
if (fail > 0) { console.error('ABORT'); process.exit(1); }
fs.writeFileSync(FILE, html, 'utf8');
console.log('OK: v0.9.9 updated, size =', html.length);
